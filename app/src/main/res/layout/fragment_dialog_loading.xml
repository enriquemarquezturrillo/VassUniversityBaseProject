<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="15dp"
    tools:background="@drawable/baseproject_gradient_vertical_2e60c0_0b2a64">

    <ImageView
        android:id="@+id/ivLoading"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/loading"
        app:layout_constraintBottom_toTopOf="@id/tvLoading"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:tint="@color/orangeLightD2A14C"
        android:contentDescription="@string/loading_text" />

    <TextView
        android:id="@+id/tvLoading"
        style="@style/textLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/loading_text"
        android:textColor="@color/orangeLightD2A14C"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ivLoading" />

</androidx.constraintlayout.widget.ConstraintLayout>